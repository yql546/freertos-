
操作多个led。待手敲？

1.

定义结构体：
封装led变量。

2.。
定义数组。

3.

初始化数组的参数

void vLedDeviceInit(stLedDeviceParamTdf *pstInit, uint8_t ucDevNum)
astLedDeviceParam[ucDevNum].pstGpioBase = pstInit->pstGpioBaseli


4.使用数组参数来写入底层函数，led_on.led-OFF
实现对led的控制

void vLedon (uint8_t ucDevNum)
if(astLedDeviceParam [ucDevNum] .ucOnLevel == 0)
HAL_GPIO_WritePin(astLedDeviceParam[ucDevNuml ·pstGpioBase， astLedDeviceParam [ucDevNum] .usGpioPin, GPIO_PIN_RESET) ;
else....


5.
在更上层，对所有led进行初始化，调用底层借口
void vLedInit ()
{
    stLedDeviceParamTdf stInit;
    stInit.pstGpioBase = GPIOC;
    stInit.usGpioPin = GPIO_PIN_13;
    stInit.ucOnLevel = 0;
    vLedDeviceInit(&stInit, 0);
    以及led1,2.3.....
}

为了更直观，宏定义，将索引值0,1,2定义为LED1,LED2,LED3
#define LED1 0


6.
然后，在主程序调用。vLedon（i）
vled_off(i)
因为，有了索引值
for循环，实现流水灯