
1.优化方法：设备号枚举
，
·1.1问题：
原先的宏定义，
设备号定义为数字常量，没有类型保护


1.2 解决办法
使用枚举定义:
自动按循序赋值
有类型纠错提示

注意：函数的参数，修改为枚举类型的形参变量。
我这里，定为：emDevNum


2.使用配置文件。来存放宏定义
2.1模块调用宏定义。
方便，在一个文件中，通过修改多个模块的宏定义，实现功能修改。

编译报警提示：
vLedon((emLedDevNumTdf)i）;
由于，i是uint_8型的。与函数的形参的枚举类型不同。
直接使用强转，类型来解决警告



3.点亮时电平参数枚举
3.1问题：
点亮时电平参数使用数字【0】【1】不直观，类型保护不足

3.2解决方法：枚举定义
优势：
命名自注释
类型保护效果好.
我依然使用，枚举定义，封装0,1.
typedef enum
{

    ledlevel_low  =0,
    ledlevel_high,
}ledlevel



使用typedef为结构体起别名（显式定义），通过别名定义结构体变量。
enum也是如此。：
#define emLedOnLevelTdf  emOnLevel;\
结构体：
stLedDeviceParamTdf

{
里面把各个变量，赋值

}
