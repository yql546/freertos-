
目的：这个up讲的是自己的理解。
没有讲解如何看手册，对自己的要求，就是学会看传感器手册，来理解
协议，并配置

驱动的封装，内部用到了的hal库：函数，代码需不需要看？
这里，先不看，不是因为up没有将，而是我的目的是学习驱动封装实录，
底层原理必须学习，但不是现在
1.
dht11是温湿度传感器，
有4个引脚：
vdd，供电3.3-5v
gnd，接地
data,单总线，传输数据
nc，不连接

2.
D H T 11的典型应用电路是没有集成上拉电阻的，
如果买的传感器模块没有没有上拉电阻，需要自己外接一个

模块的电平输入信号有有波动，会影响到数据的测量





3.
传感器模块与开发板的通信时序与通信过程：

脉冲信号分为握手信号与数据信号

握手信号：
由于，dht11默认为低功耗状态
所以，开发板将电平拉低，作为开始信号，dht11变为高速模式。
并释放电平。开发板将连接data的引脚切换为输入，等待接收数据

dht11,将电平拉低，作为响应，并释放，为数据传输作准备
接下来开始数据的传输，
dht11拉低电平，然后释放电平，作为1bit数据位，
若低电平时间大于高电平时间，则数据位为0
若低电平时间小于高电平时间，则数据为位1

数据结构：
湿度整数部分8bit+湿度小数部分8bit+温度整数部分8bit
+温度小数部分8bit
共40bit数据位。

发送完数据，dht11将电平拉低，并开始下一次数据采集
开发板将电平释放，为dht11传输数据作准备。



4.






5.
根据，时序表与参数，整理得到开发板的配置步骤：
：
1.DATA引脚保持输出高电平2S
2.DATA引脚保持输出低电平15us
3. DATA 引脚改为输入，等待应答信号
4.DATA引脚读取40个BIT
5.DATA引脚输出高电平
见手册第5页


二;
1.驱动设计流程
·1.1搭建框架：
.h文件：
定义设备号枚举
定义设备参数结构，其中包含静态数据结构和运行数据结构

.c文件：

定义设备参数数组
定义参数获取函数
定义初始化函数

由于，不同的驱动文件结构一样，我这里自己拷贝led驱动，并修改：
1.复制LED驱动代码
2.删除多余部分
3.替换名称

• 2.2基础静态参数：
•增加引脚信息
2.3基础运行参数：
，增加温，湿度存储参数


2.4基础操作函数：
引|脚方向切换
发送起始信号
等待响应信号
读取并校验数据
结束释放总线

• 2.5高级操作函数：
将基础操作函数的所有功能都组织起来并实现，就是高级操作函数
• 读取一次完整数据

这些函数的功能实现，需要完整的代码来实现完整的流程。

我这里，再次读一遍，时序图，与参数表，
来理解开发板的配置流程。

写了一个配置流程图：

c:\Users\yql\Documents\xwechat_files\wxid_k7tb4yt8lkpn22_fd0a\temp\RWTemp\2026-06\358f113982cd2104ec7b3c06211e3ac7\387293b5a9eb97fe2537a629dfbad211.png


三，驱动的配置

1，搭建基础框架
移植led驱动，进行修改，得到dht11驱动的框架
a.删除led自身的定义，枚举，结构体，函数，等等
需要保留，led设备相关的配置，（！）

b.替换参数，函数,等等的名称。.c.h文件分别替换，防止修改其他文件
led->dht11
Led->Dht11
LED->DHt11
这是提现了规范性的：
分别对应不同的含义，
led是文件名
Led是函数，参数等等代码逻辑
LED是设备名

c.添加dht11的参数。
大的层面分为，静态与动态参数两类。

然后，就是，编译，有问题，解决问题，就行。


2.封装基础操作的函数

分别封装，设置信号引脚为输入，与输出的函数

实际上，是对hal库的HAL_gpio_init函数的内部进行自己的封装